# Server port (PM2 / next start)
PORT=3000

# Backend API — same EC2 IP or domain, no trailing slash
NEXT_PUBLIC_API_URL=http://YOUR_SERVER_IP:5000/api

# Production: always false
NEXT_PUBLIC_USE_MOCK_AUTH=false
